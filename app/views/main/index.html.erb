        <div id="content-main">
          <div class="module">
            <table summary="Models available in the application.">
              <caption>
                <%= link_to("Models", url(:merb_admin_dashboard), :class => "section") %>
              </caption>
              <% @abstract_models.each do |abstract_model| %>
              <tr>
                <th scope="row">
                  <%= link_to(abstract_model.model.to_s, url(:merb_admin_list, :model_name => abstract_model.param_name)) %>
                </th>
                <td>
                  <%= link_to("Add", url(:merb_admin_new, :model_name => abstract_model.param_name), :class => "addlink") %>
                </td>
                <td>
                  <%= link_to("Edit", url(:merb_admin_list, :model_name => abstract_model.param_name), :class => "changelink") %>
                </td>
              </tr>
              <% end %>
            </table>
          </div>
        </div>
